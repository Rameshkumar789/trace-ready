"""Operational CLI scripts: build the regulatory knowledge base and seed Supabase.

Run from the ingestion/ directory in module mode so the package and the repo-root data/
folder both resolve, e.g.:  python -m scripts.ops.ingest --help
"""
